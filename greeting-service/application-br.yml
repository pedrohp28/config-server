server:
  port: 8080
greeting-service:
  greeting: "BR"
  default-value: "DEMAIS"
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always